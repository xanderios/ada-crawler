{
  "name": "ada-crawler-monorepo",
  "private": true,
  "type": "module",
  "scripts": {
    "scan": "pnpm --filter crawler scan",
    "report": "pnpm --filter crawler report",
    "web": "pnpm --filter web dev",
    "web:build": "pnpm --filter web build"
  }
}
